// import axios from 'axios';
// import apiClient from '../../apiClient';

// const FileSave = async (projectId: number, fileId: number, content: string, name: string) => {
//   try {
//     const baseURL = import.meta.env.VITE_API_BASE_URL;
//     const result = await axios.put(`${baseURL}/projects/${projectId}/files/${fileId}`, {
//       name: name,
//       content: content,
//     });
//   } catch (error) {
//     console.log(error);
//     throw error;
//   }
// };

// export default FileSave;
